[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "leetpath"
version = "1.0.0"
requires-python = ">=3.10"
dependencies = [
  "typer",
  "rich",
  "requests",
  "beautifulsoup4"
]

[project.scripts]
dsa = "leetpath.cli.main:app"
